[project]
name = "jarvis-pel"
version = "0.1.0"
description = "LiveKit Jarvis + Prompt Execution Layer"
requires-python = ">=3.10"
dependencies = [
  "livekit-agents[google,silero,elevenlabs]>=1.2.0",
  "python-dotenv",
  "playwright",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
